# Talkie-Putty runtime dependencies (Python 3.10+, Windows)
# GPU Whisper backends additionally need CUDA runtime DLLs; faster-whisper /
# ctranslate2 pull the nvidia-* CUDA wheels in transitively.

numpy==2.4.6
sounddevice==0.5.5
keyboard==0.13.5
sherpa-onnx==1.13.2
faster-whisper==1.2.1
